Place your professional app icon image as `app_icon.png` in this folder.

- Format: PNG
- Recommended size: 1024x1024 pixels
- Use a square image with a transparent or solid background

After adding the image, run:

```bash
flutter pub get
flutter pub run flutter_launcher_icons:main
```
